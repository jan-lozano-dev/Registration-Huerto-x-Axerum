# Copia aquest fitxer a .env.local i afegeix les teves variables
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Token secret per accedir a GET /api/registre (llista d'inscrits)
ADMIN_SECRET=canvia-aquest-valor-per-un-de-segur
